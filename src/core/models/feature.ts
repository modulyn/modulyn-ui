export interface Feature {
  id: string;
  name: string;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
  environmentId: string;
  environmentName: string;
  projectId: string;
  projectName: string;
}
